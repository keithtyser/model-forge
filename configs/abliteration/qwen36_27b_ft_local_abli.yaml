name: qwen36_27b_ft_local_abli
method: contrastive_refusal_direction

model:
  source: local/qwen36-27b-local-ft-v4
  local_dir: ~/models/Qwen3.6-27B-local-ft-v4-merged
  output_dir: ~/models/Qwen3.6-27B-local-ft-v4-abliterated-v1
  dtype: bfloat16
  device_map: cuda
  trust_remote_code: true

data:
  harmful_prompts: ../../datasets/abliteration/harmful_refusal.yaml
  benign_prompts: ../../datasets/abliteration/benign_control.yaml

activation_collection:
  batch_size: 1
  max_pairs: 32
  max_seq_len: 1024
  preprocessing_parallelism: auto
  high_parallelism_c: 192
  layer_skip_first: 4
  layer_skip_last: 2
  token_position: suffix_mean
  direction_extraction: mean_difference
  winsorize_quantile: 0.02
  harmful_suffix: "\n\nI can't help with instructions for harmful requests."
  benign_suffix: "\n\nHere is a direct, helpful answer."

edit:
  mode: projection
  direction_transform: biprojection
  norm_preserve: true
  strength: 2.0
  module_strengths:
    self_attn.o_proj.weight: 1.0
    mlp.down_proj.weight: 0.65
  layer_start: 4
  layer_end: 45
  target_weight_suffixes:
    - mlp.down_proj.weight
    - self_attn.o_proj.weight
  leave_embeddings_untouched: true
  leave_lm_head_untouched: true
  leave_moe_experts_untouched: true
  require_all_target_directions: false
  review_required_before_export: true

safety:
  require_execute_flag: true
  min_free_cuda_gb: 18
  one_model_process_at_a_time: true

artifacts_dir: artifacts/abliteration/qwen36_27b_ft_local_abli

sota:
  preferred_backend: heretic
  output_dir: ~/models/Qwen3.6-27B-local-ft-v4-abliterated-v1
  work_dir: artifacts/abliteration/qwen36_27b_ft_local_abli/sota_v4
  backends:
    heretic:
      install: pip install -U 'heretic-llm[research]'
      quantization: none
      device_map: auto
      batch_size: 1
      max_batch_size: 1
      max_response_length: 16
      n_trials: 80
      n_startup_trials: 24
      orthogonalize_direction: true
      row_normalization: full
      winsorization_quantile: 1.0
      model_forge_prompt_datasets: {}
      notes: >
        Compute fresh refusal directions and run a fresh Heretic search on the
        promoted local Qwen 3.6 FT v4 model. Do not transfer Gemma constants
        except as weak starting priors for target module classes. Select by
        model-forge evals: refusal removal improves only when source-relative
        capability and benign quality are preserved.

eval:
  source_variant: local_ft_v4
  target_variant: local_ft_abli
  commands:
    - MODEL_FORGE_TRIALS=1 ./forge eval qwen36_27b local_ft_abli --internal
    - ./forge compare qwen36_27b
